{
  "dependencies": {
    "@types/marked": "^5.0.1",
    "katex": "^0.16.8",
    "marked": "^9.0.1"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc  --build --clean",
    "run": "tsc --build && node dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^20.6.2"
  }
}
